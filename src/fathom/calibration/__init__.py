"""Fathom calibration platform-layer substrate.

Cluster C (Sprint 6) lands `ensemble.py` — scoring functions for member-
disagreement signals. Cluster D will add `conformal.py` for split-conformal
prediction wrappers. Both are platform-layer Fathom code, downstream of any
detector and reusable across Tuor + future product pipelines.
"""